version: "3.3"

services: 

  dbdelta:
    env_file:
      - env.list
    image: vitorta/dbdelta
    volumes:
      - voldbdelta:/var/lib/mysql
    networks:
      - delta   

  nodedelta:
    env_file:
      - env.list
    image: vitorta/nodedelta
    depends_on: 
      - dbdelta
    networks:
      - delta
     
  nginxdelta:
    image: vitorta/nginxdelta 
    ports:
      - "80:80"
    depends_on: 
      - nodedelta   
    networks:
      - delta      
volumes:
  voldbdelta:
networks:
  delta: